# dependencies (all levels — incl. local skill tooling)
/node_modules
node_modules/
/.pnp
.pnp.js

# local Claude Code tooling (QA skills) — not part of the deliverable
.claude/skills/
.claude/settings.local.json

# local agent tooling — playwright node_modules를 품고 있어 커밋 대상이 아니다
/.agents/

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# typescript
*.tsbuildinfo

# vercel
.vercel

# CSR 수집 원본 기록 — 저장소 산출물 아님(별도 보관, 로컬 참조용)
/.local/

# 계측·회귀 테스트 산출물(스크린샷·리포트) — 재생성 가능하므로 커밋 대상 아님
/audit/
/test-results/
/playwright-report/

# redundant 16MB data copies — canonical copy is public/downloads/
/KG에듀원_과정리스트.xlsx
ref/prototype/KG에듀원_과정리스트.xlsx
