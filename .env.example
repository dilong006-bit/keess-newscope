# KEESS — 공개 환경변수 예시 (값 없이 형태만. 실제 값은 배포 환경에 설정)

# 인재키움 프리미엄 원타임 티저 스낵바 (모바일 신규 메뉴 인지 개선 명세서 v1.0 §2 M3)
# on  = 모바일 첫 진입 1.2초 뒤 하단 스낵바 1회 노출
# off = 렌더하지 않음 (기본값 · 미설정 시에도 off 로 동작)
NEXT_PUBLIC_KIUM_TEASER=off
